apiVersion: apps/v1
kind: Service
metadata:
  name: self-healing-service
  namespace: self-healing-system
spec:
  selector:
    app: self-healing-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: NodePort
